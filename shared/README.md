# shared

本目录用于存放手机端和 MatePad 端共用的资源与规则说明。

## 主要内容

1. 公共 UI 素材；
2. 邮差鸟、信封、树、草地、云朵等 PNG 素材；
3. APP 图标资源；
4. 两端共用的邮件命名规则；
5. 两端共用的附件格式规则；
6. 两端共用的版本命名规则。

## 安全规则

本目录只能存放非敏感资源。

不得存放 QQ 邮箱授权码、邮箱密码、GitHub token、私有 API token 或任何个人配置。
