name: Deprecated - Publish Mobile APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Stop deprecated workflow
        run: echo "Deprecated workflow disabled. Use Phone Update Email Auto for phone APK email updates."
