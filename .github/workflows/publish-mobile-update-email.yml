name: Deprecated - Publish Mobile Update Email

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Stop deprecated workflow
        run: echo "Deprecated workflow disabled. Use Publish Phone 0.4.6 Test or later phone test workflow."
