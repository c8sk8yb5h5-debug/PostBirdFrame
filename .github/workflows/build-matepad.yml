name: Build MatePad APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'matepad-app/**'
      - '.github/workflows/build-matepad.yml'

jobs:
  build-matepad:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10.2

      - name: Build MatePad debug APK
        run: gradle -p matepad-app assembleDebug

      - name: Upload MatePad APK
        uses: actions/upload-artifact@v4
        with:
          name: PostBird-MatePad-debug-apk
          path: matepad-app/app/build/outputs/apk/debug/*.apk
